import { PowerTotalPipe } from './power-total-pipe';

describe('PowerTotalPipe', () => {
  it('create an instance', () => {
    const pipe = new PowerTotalPipe();
    expect(pipe).toBeTruthy();
  });
});
